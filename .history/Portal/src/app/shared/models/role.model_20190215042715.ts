export class Role {
    _id: string;
    roleId: number;
    role: string;

    // deserialize(data: any): Role {
    //     return <Role>Object.assign({}, {
    //         _id: data._id,
    //         roleId: data.roleId,
    //         role: data.role
    //     });
    // }
}

